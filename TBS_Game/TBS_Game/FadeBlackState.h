#ifndef FADEBLACKSTATE_H
#define FADEBLACKSTATE_H
/*
#include "IGameState.h"

class IStateBasedGame;

class FadeBlackState
{
public:
	FadeBlackState();
	//toBlack refers to whether the state should fade the screen to black or from black
	FadeBlackState(unsigned int time, bool toBlack);
	~FadeBlackState();
	void init();
	void cleanup();

	void pause();
	void resume();

	void handleEvents(IStateBasedGame& game);
	void update(IStateBasedGame& game);
	void draw(IStateBasedGame& game);

private:
	sf::Texture m_blackTexture;
	sf::Sprite m_blackSprite;
};

*/

#endif